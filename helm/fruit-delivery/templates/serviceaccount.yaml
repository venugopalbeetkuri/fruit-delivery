{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fruit-delivery.serviceAccountName" . }}
  namespace: {{ include "fruit-delivery.namespace" . }}
  labels:
    {{- include "fruit-delivery.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
